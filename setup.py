from setuptools import setup, find_packages

setup(
    name="llmops-rag",
    version="1.0.0",
    description="Enterprise LLMOps RAG System",
    author="Ayorinde",
    packages=find_packages(),
    python_requires=">=3.11",
    install_requires=[],  # Dependencies are in requirements.txt
)
